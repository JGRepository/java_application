<!DOCTYPE html>
<html 
      xmlns:th="https://thymeleaf.org"
      xmlns:layout="http://www.ultraq.net.nz/thymeleaf/layout">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edição de Estados</title>

    <!-- Link para o Bootstrap (CDN) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KyZXEJ12N8Exm5jIh0PnJ4drjGb5q2js9ihWbxZIxWExYQqtGhpBkb84h8wAnFdw" crossorigin="anonymous">
</head>
<body>
<div class="container mt-4">
    <h1 class="mb-4">Edição de Estados</h1>

    <table class="table table-bordered">
        <thead class="thead-dark">
        <tr>
            <th>ID</th>
            <th>Nome Estado</th>
            <th>Sigla</th>
            <th>Ação</th>
        </tr>
        </thead>
        <tbody th:each="estado : ${listaEstados}">
        <tr>
            <td th:text="${estado.id}"></td>
            <td th:text="${estado.nome}"></td>
            <td th:text="${estado.sigla}"></td>
            <td>
                <a th:href="@{/editarEstado/{id}(id=${estado.id})}" class="btn btn-primary btn-sm">Editar</a>
                <a th:href="@{/removerEstado/{id}(id=${estado.id})}" class="btn btn-danger btn-sm">Remover</a>
            </td>
        </tr>
        </tbody>
    </table>
</div>

<!-- Script do Bootstrap (opcional, para funcionalidades JS como dropdowns, modais, etc.) -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js" integrity="sha384-pzjw8f+ua7Kw1TIq0YQeoP0KjhZSL4aM9cD0mY7v7n01B7kZOwKwXebqmxzSA5w1" crossorigin="anonymous"></script>
</body>
</html>
